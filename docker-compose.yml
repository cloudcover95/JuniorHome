# path: docker-compose.yml

version: '3.8'

services:
  juniorclimbs:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
