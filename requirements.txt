# path: requirements.txt

# Core (keep minimal for lightning speed)
pydantic>=2.0
qrcode[pil]>=7.4  # Optional for QR generation
# For future BitNet integration:
# mlx>=0.15  # Apple Silicon
# numpy>=1.24
