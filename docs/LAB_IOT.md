# Lab IoT

Catalog names common local stacks (Moonraker, OctoPrint, GRBL, CUPS, Pi, ROS2).
Home writes `~/.juniorhome/lab/spool/<device>-<name>` only.
`fire: false` — no HTTP POST to :7125, no laser cycle, no GPIO from automations.
The machine firmware / OctoPrint UI / CUPS is what starts the job.
