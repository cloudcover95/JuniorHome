# Native rails

Linux app: `ui/*.html` + `juniorhome.desktop` if present + loopback only.
OSai: `linux_app.json` rejects 0.0.0.0.

iOS: JuniorLLM `rails/ios/Handshake.swift` is a struct, not a shipping IPA.
OSai: `ios.json` portrait + no-cloud-llm.

Same trit pack as T0. Same protocol.
