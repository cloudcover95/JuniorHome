# Climbs Gaia port (pollinate only)

JuniorClimbs now ships domain-agnostic `core/trit_unpack.py` and `core/gaia_threads.py`.

Home remains the index. Climbs remains a vertical. Poker remains the felt.

```
JuniorHome/scripts/climbs_gaia_prod.py
  → JuniorClimbs.core.gaia_threads.GaiaPool
  → envelope area ∈ {home, climbs, poker, quant, ...}
```

Layers stay `who spine dash view walk mesh`. `ue5_launch` and `download` stay false.
BitNet-mlx is not replaced. Compute stays JuniorLLM when the ports package is on PYTHONPATH.
