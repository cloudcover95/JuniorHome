# JuniorCrowdMesh + JuniorFieldBitNet

Offline-first decentralized field forum + ternary BitNet heads for StoneField/NavMesh.
Lives in JuniorClimbs 0.8.0-edge.

- Envelopes stored locally, hashed ids, bundle export/import for peer/sneakernet sync
- No central forum host, no MP/KAYA links
- FieldBitNet labels: beta_trust, access_caution, tenure_usfs, tenure_private, nav_priority

Routes: `/crowd/*` and `/fieldbitnet/*`
