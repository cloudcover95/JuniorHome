# JuniorOS handheld — 10 m DEM + Omega dry-run

```
python3 web3node/junioros_feature.py
```

USGS EPQS ~10 m point grid (Flagstaff default, StoneField Boulder optional).
Omega `omega-lidar` job staged when Blender is absent.
T4 dem-slice. No UE boot.
