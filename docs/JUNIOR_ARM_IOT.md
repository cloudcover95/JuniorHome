# JUNIOR_ARM_IOT

JuniorHome pack pointer. Implementation is `packs/junior_arm_iot`.
BitnetCloud sidecar pointer is `packs/bitnetCloud`.

Jobs: `iot-host`, `tritarm-guest`, `fieldcore-intent`, `frameforge-pad`.

LOD trit: -1 retreat / 0 hold / +1 approach. CPU intent only.

CI never requires Blender, MLX, or UnrealEditor.

```
python3 -m packs.junior_arm_iot.cli demo
python3 -m packs.junior_arm_iot.cli serve-intent --port 8777
```

Sisters: BitNet-mlx, JuniorMemSys-Suite, JuniorOmega, AGI_SDK, FrameForge,
FrameForge2D, crispy-mouse, JuniorLLM, JuniorDrive.

Not a Nintendo product.
