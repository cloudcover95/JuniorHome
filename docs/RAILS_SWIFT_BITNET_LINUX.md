# Rails — Swift + BitNet Linux OS overlay

JuniorLLM `rails/linux/backend.py` picks mlx / cuda / cpu from the box.
AbsMean trits are the same on ARM and x86. Not a custom kernel.

```
PYTHONPATH=../JuniorLLM python ../JuniorLLM/scripts/os_probe.py
```

Also: bitnetd.service, juniorctl, Guardrail.swift, layer_mgr on Teqp phase.
