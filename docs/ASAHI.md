# Asahi in the Home suite

Facts (2026-09): M3 support announced with GPU/power caveats; DCP/sleep missing on M3; m1n1 1.6 does GPU init in stage2 (Rust). Neural Engine out of tree.

Home action: `rails.linux.backend.probe().asahi`. Goldend cases in JuniorLLM `junior_osai/goldens/asahi.json`.
Do not ship an Asahi remix ISO from this repo.
