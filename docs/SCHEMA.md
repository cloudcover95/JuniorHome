# Schema

Collections: tickets, poses, pulses, nodes.
Stored as vault/mongo_shape.jsonl. Indexes listed for a future Mongo daemon.
JuniorDrive (ours) has no OAuth. galkreiser1 auth is Google login for a job board — not used.
