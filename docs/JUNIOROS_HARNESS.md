# JuniorOS harness

For boxes like this one: Linux x86_64, numpy, no MLX, no nvidia-smi, no blender.

```
bash web3node/check_junioros_harness.sh
```

T0 home-clock. UE off. Omega stage only. BitLinear + trit pack + agent graph.
Uses cached Flagstaff DEM + live_book if present. Does not re-hit EPQS/Yahoo.
