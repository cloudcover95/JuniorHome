# BitnetCloud

The name is the joke. The store is `vault/bitnetCloud/index.jsonl`.
`cloud: false`, `local: true`, hook only on 127.0.0.1.
JuniorCloud LLC is the company. BitnetCloud is this vault folder.

## Sidecar (0.2.0)

Implementation: `packs/junior_arm_iot` (TritARM host + FieldCore intent).
Pointer: `packs/bitnetCloud`.

Does not rewrite FrameForge / FrameForge2D kernels.
BitNet scores CPU intent only. `python/frameforge/math_kb.py` owns knockback.

SIS: JuniorMemSys-Suite `junior_memsys_suite.lattice.msis_commit` when on
path, else local palace.

HMI: crispy-mouse packets → pad MMIO → `xr_beta` for JuniorDrive.
UnrealEditor does not boot on the 45 W node.

Formats: TritARM executes. `.xci .nsp .nca .cia .3ds .nds .gba .z64`
classify / public header only. No decrypt. Not a Nintendo product.

UMR (hanyang9/UMR) is not vendored — no LICENSE file on that tree.
Omega still owns the Blender pipeline.
