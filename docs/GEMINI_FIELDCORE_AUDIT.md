# Gemini FieldCore + proto dumps

Q: parquet on edge vs JuniorCloud cluster?
A: local only. `~/.juniorhome/gaia_mesh/fieldcore_spine.jsonl`. No cluster, no S3, no parquet dep.

SVD was not dropped. Live path is JuniorLLM `ports.svd_tick` (numpy or power). web3node posters still show retain-k. Home does not pull stocksnode SVD into a new MLX 1024² job.

Rejected from this dump: mlx+pyarrow+toml FieldCore class, handshake rewrite, JuniorLLM.agents.coach fake modules, 02_Assets, Live Engine blender.
