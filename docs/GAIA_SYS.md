# JuniorGaia system

Not a downloaded model. Not Halo. Not climbing-only.

```
end user
  ui/dash.html     orientation + scale (optional portrait)
  ui/gaia.html     companion face (optional)
       |
  scripts/gaia_sys_prod.py
       |
  JuniorLLM ports.gaia_sys.system()
       |
  +-- who     ~/.juniorhome/gaia.json
  +-- spine   winsor bolts
  +-- dash    any note → area → I2_S
  +-- view    {w,h,orient} → Omega dash-viewport
  +-- walk    StoneField only if the note asks
  +-- mesh    SVD OBJ when cad/terrain
       |
  AGI_SDK core.gaia_port list only; compute stays JuniorLLM
```

Probes: llama/bitnet.cpp ready only with files on disk.
