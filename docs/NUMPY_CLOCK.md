# Numpy clock

Do not replace numpy on T0 Linux. MLX is off. CUDA is off.
Custom kernel is AbsMean + BitLinear + trit pack (`ternary_kernel.py`).
List path = handheld/IoT. Numpy path = matrices.
SIS/stb/palace stay optional above the pack.

```
python3 web3node/sandbox_suite.py
```
