# Ecosystem sync (2026-09-09)

Source of truth for local LLM suite + JuniorOS overlay + AIE + overnight job:
`cloudcover95/JuniorLLM` (`docs/BETA_TO_OS.md`, `rails/linux/`, `junior_aie/`).

Overnight automation: Grok Automations task `JuniorCloud overnight build` daily America/Denver.
This repo orchestrates products; it does not vendor those trees.

Hub README is the public map (matches the GitHub description: 45 W local-first edge home, BitNet b1.58, TDA/SVD, LIF, HDC).
FrameForge / FrameForge2D are game drop-ins only — see README § Games.
