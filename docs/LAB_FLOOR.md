Optional Home module. Same shape as StoneField (jsonl nodes), different domain (printers/mills/calcs).
Companion HTML is a local page, not Halo Cortana.
Omega: file job only. Machines stay 127.0.0.1 until the owner maps a real host.
