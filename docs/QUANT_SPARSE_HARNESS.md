# Quantization, sparse formats, modular harness

JuniorLLM `junior_bitnet/math.py` stays the written definition: AbsMean 1.58, AbsMax act, sign-binarize.
StocksNode `Web3FinancialTensor` stays the manifold. Home routes the task name through JuniorLLM pick.
Sparse: COO / CSR / CSC / trit-CSR / 2-bit trit pack. No scipy.

```
bash web3node/check_modular_harness.sh .
```
