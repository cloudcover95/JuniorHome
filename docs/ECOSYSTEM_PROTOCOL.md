# JuniorHome × JuniorOS — what is live vs missing

Protocol: goldend-osai-omega/1
Schema: JuniorLLM junior_osai/schema/gaia.system.json (now includes trit ∈ {-1,0,1}, gamma, i2s_hex, identity.issuer=local-gaia.json)
1.58: winsor pack on the note; I2_S hex on the envelope. Not a 2B checkpoint.

## JuniorOS
There is no separate cloudcover95/JuniorOS repo. OS track is:
- JuniorLLM rails/linux (i2sd, llama/bitnet probes, overlay)
- JuniorHome os_route + trit_tick
ISO / vmlinuz / Asahi patch / GGUF pull = not shipped.

## UE5 × Blender
ports.ue5_port.scene → JSON actors
ports.ue5_blender.write → ~/.juniorhome/gaia_mesh/ue5_actors.py
AGI_SDK blender_bridge.gaia_cmd *names* blender --background; Home automations do not run it.
Spawning in-editor Unreal actors is not implemented. launch=false.

## Scripted surfaces
dash, companion, field, markets, clock, handshake, walk, terrain OBJ, markets tiles.

## Pointer-only (docs, no runtime import wired in Home)
JuniorCoach, JuniorPiThon, JuniorPiPython, JuniorSOL, JuniorSolana,
JuniorAGI_SDK, BitNet-mlx, web3node (standalone repo), stocksnode,
FrameForge, FrameForge2D, crispy-mouse.

Climbs / Fetch / EngrTools / Quant / Python-Suite have protocol markdown only.

## Will not appear as Home runtime without an operator drop
live market feed, USGS/Cesium, Open-Elevation docker, UE5 editor, BitNet 2B GGUF, Metal in Home.
