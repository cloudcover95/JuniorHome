# FieldCore + Flagstaff

JuniorHome keeps the home clock. This pack only *names* the other trees.

| Tree | Role here |
|---|---|
| JuniorLLM | `JuniorBitNetFieldCore` default port — crowd/field trit scorer |
| Flagstaff | Public municipal FieldCore node (city centroid, not a private pin) |
| stocksnode | `Web3FinancialTensor` manifold when importable; local numpy shape otherwise |
| web3node | SVD retain → residual → `TernaryTDAMesh.tick` |
| JuniorMemSys-Suite | JSONL palace row (`fieldcore_flagstaff.jsonl`) |
| AGI_SDK | `ModelRouter` apple_silicon / ternary — not vendored |

FrameForge knockback is unchanged. FieldCore logits are intent / access labels only.

```
python3 web3node/fieldcore_bridge.py
```
