# BitnetCloud sidecar

JuniorHome vault name: `vault/bitnetCloud`. Pack name: `packs/bitnetCloud`.
Implementation lives next door in `packs/junior_arm_iot` (TritARM host).

`cloud: false`, `local: true`, hook only on 127.0.0.1.

This folder is a pointer + JSON contract. It does not rewrite FrameForge
kernels. FieldCore scores CPU intent. `math_kb.py` owns knockback.

```
python3 -m packs.junior_arm_iot.cli demo
python3 -m packs.junior_arm_iot.cli serve-intent --port 8777
```

Sisters: JuniorMemSys-Suite (SIS), crispy-mouse (pad), JuniorOmega (Blender),
JuniorLLM (input×function), JuniorDrive (xr_beta). UMR was not vendored —
no LICENSE on hanyang9/UMR.

Not a Nintendo product. JuniorCloud LLC.
